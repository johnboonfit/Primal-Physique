// The Community tab — same shared screen the coach reaches via a
// NavCard push (../community/index.tsx handles both audiences off
// profile.role already), just registered here too so it's a real
// persistent tab for the client instead of a screen pushed from Home.
export { default } from '../community/index';
